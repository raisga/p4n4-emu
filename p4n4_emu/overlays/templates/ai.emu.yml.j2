# Generated by p4n4-emu — DO NOT EDIT
# Profile: {{ profile.name }} | {{ profile.description }}
services:
  ollama:
    {% if profile.is_arm %}
    platform: linux/{{ profile.arch }}
    {% endif %}
    deploy:
      resources:
        limits:
          cpus: "{{ "%.2f"|format(profile.cpus * 0.60) }}"
          memory: "{{ (profile.memory_mb * 0.55)|int }}m"
    {% if blkio_device %}
    blkio_config:
      weight: {{ profile.blkio_weight }}
      device_read_bps:
        - path: "{{ blkio_device }}"
          rate: "{{ profile.blkio_read_bps }}"
      device_write_bps:
        - path: "{{ blkio_device }}"
          rate: "{{ profile.blkio_write_bps }}"
    {% endif %}

  letta:
    {% if profile.is_arm %}
    platform: linux/{{ profile.arch }}
    {% endif %}
    deploy:
      resources:
        limits:
          cpus: "{{ "%.2f"|format(profile.cpus * 0.20) }}"
          memory: "{{ (profile.memory_mb * 0.20)|int }}m"
    {% if blkio_device %}
    blkio_config:
      weight: {{ profile.blkio_weight }}
      device_read_bps:
        - path: "{{ blkio_device }}"
          rate: "{{ profile.blkio_read_bps }}"
      device_write_bps:
        - path: "{{ blkio_device }}"
          rate: "{{ profile.blkio_write_bps }}"
    {% endif %}

  n8n:
    {% if profile.is_arm %}
    platform: linux/{{ profile.arch }}
    {% endif %}
    deploy:
      resources:
        limits:
          cpus: "{{ "%.2f"|format(profile.cpus * 0.20) }}"
          memory: "{{ (profile.memory_mb * 0.15)|int }}m"
    {% if blkio_device %}
    blkio_config:
      weight: {{ profile.blkio_weight }}
      device_read_bps:
        - path: "{{ blkio_device }}"
          rate: "{{ profile.blkio_read_bps }}"
      device_write_bps:
        - path: "{{ blkio_device }}"
          rate: "{{ profile.blkio_write_bps }}"
    {% endif %}
