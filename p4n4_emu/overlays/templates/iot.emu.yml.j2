# Generated by p4n4-emu — DO NOT EDIT
# Profile: {{ profile.name }} | {{ profile.description }}
services:
  mqtt:
    {% if profile.is_arm %}
    platform: linux/{{ profile.arch }}
    {% endif %}
    deploy:
      resources:
        limits:
          cpus: "{{ "%.2f"|format(profile.cpus * 0.10) }}"
          memory: "{{ (profile.memory_mb * 0.05)|int }}m"
    {% if blkio_device %}
    blkio_config:
      weight: {{ profile.blkio_weight }}
      device_read_bps:
        - path: "{{ blkio_device }}"
          rate: "{{ profile.blkio_read_bps }}"
      device_write_bps:
        - path: "{{ blkio_device }}"
          rate: "{{ profile.blkio_write_bps }}"
    {% endif %}

  influxdb:
    {% if profile.is_arm %}
    platform: linux/{{ profile.arch }}
    {% endif %}
    deploy:
      resources:
        limits:
          cpus: "{{ "%.2f"|format(profile.cpus * 0.40) }}"
          memory: "{{ (profile.memory_mb * 0.35)|int }}m"
    {% if blkio_device %}
    blkio_config:
      weight: {{ profile.blkio_weight }}
      device_read_bps:
        - path: "{{ blkio_device }}"
          rate: "{{ profile.blkio_read_bps }}"
      device_write_bps:
        - path: "{{ blkio_device }}"
          rate: "{{ profile.blkio_write_bps }}"
    {% endif %}

  node-red:
    {% if profile.is_arm %}
    platform: linux/{{ profile.arch }}
    {% endif %}
    deploy:
      resources:
        limits:
          cpus: "{{ "%.2f"|format(profile.cpus * 0.30) }}"
          memory: "{{ (profile.memory_mb * 0.15)|int }}m"
    {% if blkio_device %}
    blkio_config:
      weight: {{ profile.blkio_weight }}
      device_read_bps:
        - path: "{{ blkio_device }}"
          rate: "{{ profile.blkio_read_bps }}"
      device_write_bps:
        - path: "{{ blkio_device }}"
          rate: "{{ profile.blkio_write_bps }}"
    {% endif %}

  grafana:
    {% if profile.is_arm %}
    platform: linux/{{ profile.arch }}
    {% endif %}
    deploy:
      resources:
        limits:
          cpus: "{{ "%.2f"|format(profile.cpus * 0.15) }}"
          memory: "{{ (profile.memory_mb * 0.10)|int }}m"
    {% if blkio_device %}
    blkio_config:
      weight: {{ profile.blkio_weight }}
      device_read_bps:
        - path: "{{ blkio_device }}"
          rate: "{{ profile.blkio_read_bps }}"
      device_write_bps:
        - path: "{{ blkio_device }}"
          rate: "{{ profile.blkio_write_bps }}"
    {% endif %}
